services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/state/monitoring.db
    volumes:
      - monitoring_state:/app/state
    command: ["streamlit", "run", "app.py", "--server.address=0.0.0.0"]

  dashboard:
    build: .
    ports:
      - "8502:8501"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/state/monitoring.db
    volumes:
      - monitoring_state:/app/state
    command: ["streamlit", "run", "dashboard.py", "--server.address=0.0.0.0"]
    depends_on:
      - app

volumes:
  monitoring_state:
