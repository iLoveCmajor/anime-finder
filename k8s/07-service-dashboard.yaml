apiVersion: v1
kind: Service
metadata:
  name: anime-finder-dashboard
  namespace: anime-finder
spec:
  type: ClusterIP
  selector:
    app: anime-finder
    component: dashboard
  ports:
    - name: http
      port: 80
      targetPort: 8501
